name: integrity_dashboard
description: Test viewing the integrity dashboard and metrics
goal: |
  Navigate to the integrity dashboard by pressing '7' or selecting Integrity from the sidebar.
  The integrity view should show the current grade and any metrics in the data panel.
  Explore the integrity information, then use Tab to return focus to the prompt.

preconditions: []

success_criteria:
  - screen:MainScreen
  - no_errors
  - completed

max_steps: 15
timeout_seconds: 45

tags:
  - integrity
  - dashboard
