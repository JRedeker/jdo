name: integrity_dashboard
description: Test viewing the integrity dashboard and metrics
goal: |
  Navigate to the integrity dashboard by pressing 'i' from the home screen.
  The integrity view should show the current grade and any metrics.
  Explore the integrity information, then return to home using escape.

preconditions: []

success_criteria:
  - screen:HomeScreen
  - no_errors
  - completed

max_steps: 15
timeout_seconds: 45

tags:
  - integrity
  - dashboard
