name: planning_hierarchy
description: Test viewing and navigating the planning hierarchy
goal: |
  View the planning hierarchy by pressing '6' or selecting Hierarchy from the sidebar.
  The hierarchy view should show visions, goals, milestones, and commitments
  in a tree structure in the data panel. Navigate around the hierarchy if items exist.
  Use Tab to cycle focus between sidebar, prompt, and data panel.

preconditions: []

success_criteria:
  - screen:MainScreen
  - no_errors
  - completed

max_steps: 20
timeout_seconds: 60

tags:
  - hierarchy
  - planning
  - navigation
